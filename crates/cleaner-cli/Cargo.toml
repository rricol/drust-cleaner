[package]
name = "cleaner-cli"
version = "1.0.0"
edition = "2024"

[[bin]]
name = "cli-folder-cleaner"
path = "src/main.rs"

[dependencies]
cleaner-core = { path = "../cleaner-core" }
clap = { version = "4", features = ["derive"] }
colored = "2"
anyhow = "1"
